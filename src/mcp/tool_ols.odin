package mcp_server

// Tier 2 OLS tools — planned for M6 (requires OLS type resolution).
// get_symbol and export_symbols are now implemented in tool_graph.odin.
// This file is reserved for future OLS-backed tools (hover, completions, etc.).
